version: 2
jobs:
    build:
        docker:
            - image: circleci/node:10.13
        working_directory: ~/build

        steps:
            - checkout
            - run: npm install
            - run: gulp 
            - run: git subtree push --prefix dist origin gh-pages 
