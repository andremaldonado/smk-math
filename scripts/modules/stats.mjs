"use strict";function calculateFinalScore(e,t,a,c){if(0==t)return 0;let n=t/c*100*(t/10)*(3*a/calculateTimeSpent(e));return n=Math.round(n),n}function calculateTimeSpent(e){return(Date.now()-e)/1e3}function calculateAverageTimeSpent(e,t){return calculateTimeSpent(e)/t}export{calculateFinalScore,calculateTimeSpent,calculateAverageTimeSpent};